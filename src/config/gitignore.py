"""Settings for building the gitignore file."""

# The pattern for valid folder names (use None for the default)
FOLDER_PATTERN = None
# The pattern for valid file names (don't include the extension, use None for the default)
FILE_PATTERN = None

# Allow these directories (don't include the trailing "/")
ALLOW_DIRECTORIES = []
# Ignore these directories (don't include the trailing "/")
IGNORE_DIRECTORIES = []
# Allow files with these extensions (don't include the leading ".")
ALLOW_EXTENSIONS = []
# Allow these specific files
ALLOW_FILES = []
# Ignore these specific files
IGNORE_FILES = []

# Add this to the end of the gitignore file
TEXT = ""